  # if nvm is available e.g. nvm ls.  if it works, if not then try nvm script.
  # then it should check for the desired nodejs version is installed
  # then it should check if the desired version is in use.
