({
  "ts": {
    "compilerOptions": {
      "allowJs": true
    }
  },
  "logLimit": 200,
  "delay": 1500
})

// The above must be placed at the top of a file for Quokka to recognize this.
// Also, if already started, you've to restart quokka.
